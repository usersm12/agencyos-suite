// Database initialization is handled via Supabase migrations.
// This file is kept as a no-op to avoid breaking imports.
export const initializeDatabase = () => {
  // No-op: schema is managed through migrations
};
